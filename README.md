# nina-media-relay

Transient handoff point. Files land here only long enough for an outside service
to fetch them, then the history is wiped. Nothing is stored here.

Process and privacy rules: `_core/media-relay/README.md` in nina-os.
